import "./Contact.scss";
import { Formik, Field, Form } from "formik";

const Contact = () => {
  const data = {
    toggle: true,
    checked: ["One"],
  };
  return <div>Contact</div>;
};

export default Contact;
